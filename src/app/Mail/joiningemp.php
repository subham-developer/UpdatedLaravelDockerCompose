<?php

namespace App\Mail;

use Illuminate\Bus\Queueable;
use Illuminate\Mail\Mailable;
use Illuminate\Queue\SerializesModels;
use Illuminate\Contracts\Queue\ShouldQueue;

class joiningemp extends Mailable
{
    use Queueable, SerializesModels;

    public $data;
    /**
     * Create a new message instance.
     *
     * @return void
     */
    public function __construct($data)
    {
            $this->data = $data;
    }

    /**
     * Build the message.
     *
     * @return $this
     */
    public function build()
    {
        return $this->from($this->data['from'])->subject('Regarding Joining : ' .$this->data['client'] )->attach(public_path().'/docs/Nimap_client_location_guidelines_updated.pdf')->view('Interviewtemplate')->with('datas',$this->data['body']); 
    }
}
